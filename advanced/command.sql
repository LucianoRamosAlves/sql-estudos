-- posso usar o cmd para executar um script no mysql
-- mysql -u root < script.sql

-- começa com mysqlsh 
-- mysqlsh  -- depois \sql para executar o script

-- \connect root@localhost para logar
--- senha

-- no cmd raiz uso -u root -p

-- mysql_config_editor set --host=localhost --user=root --password com iiso eu salvo a senha

-- depois mysql -u root, entra sem senha

-- ysql -u root -D word -p < "C:\Users\lramo\OneDrive\Documentos\Estudos\sql-estudos\objects\function\function.sql" aqui eu entro usando o arquivo